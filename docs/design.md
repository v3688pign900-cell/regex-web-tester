# Design

## New in v1.8
- Primary and compare regex inputs for side-by-side count comparison
- Replace history panel stored only in current page memory
- Named group summary aggregation section
- Mobile spacing tightened for better touch usage

## Safety model
- All processing stays in browser memory
- No remote requests
- No localStorage/sessionStorage/cookies
- No eval
